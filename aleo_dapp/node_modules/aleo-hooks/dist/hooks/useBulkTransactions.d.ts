import { AleoTransaction } from '@demox-labs/aleo-wallet-adapter-base';
export declare const useBulkTransactions: () => {
    executeBulkTransactions: (aleoTransactions: AleoTransaction[]) => Promise<string[] | null>;
    error: Error | null;
    transactionIds: string[] | null;
    loading: boolean;
};
