import { TransactionStatus } from '../types';
export declare const useTransactionStatus: () => {
    requestTransactionStatus: (transactionId: string) => Promise<TransactionStatus | null>;
    error: Error | null;
    transactionStatus: TransactionStatus | null;
    loading: boolean;
};
