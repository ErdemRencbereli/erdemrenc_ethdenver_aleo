interface IUseDecryptArguments {
    cipherText: string;
    tpk?: string;
    programId?: string;
    functionName?: string;
    index?: number;
    enabled?: boolean;
}
export declare const useDecrypt: ({ cipherText, tpk, programId, functionName, index, enabled, }: IUseDecryptArguments) => {
    decryptedText: string | null;
    loading: boolean;
    error: null;
    decrypt: () => Promise<void>;
};
export {};
