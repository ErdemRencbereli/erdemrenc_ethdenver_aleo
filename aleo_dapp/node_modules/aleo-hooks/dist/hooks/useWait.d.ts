import { TransactionStatus } from '../types';
interface UseWaitAguments {
    poolingInterval?: number;
}
export declare const useWait: ({ poolingInterval }?: UseWaitAguments) => {
    status: TransactionStatus | null;
    error: Error | null;
    wait: (transactionId: string, onTransactionStatusChange?: ((txStatus: TransactionStatus | null, stopPooling: () => void) => void) | undefined) => Promise<unknown>;
};
export {};
